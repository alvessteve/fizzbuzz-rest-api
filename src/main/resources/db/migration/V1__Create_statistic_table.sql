CREATE TABLE statistic (
    id SERIAL PRIMARY KEY,
    occurences SMALLINT,
    first_integer SMALLINT,
    second_integer SMALLINT,
    max_limit SMALLINT,
    str1 VARCHAR(10),
    str2 VARCHAR(10)
);
